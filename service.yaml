apiVersion: v1
kind: Service
metadata:
  name: myapp-service
spec:
  type: NodePort
  ports:
    - targetPort: 80 #web server port
      port: 80 #service port 
      nodePort: 30008 #external port
  selector:
    app: myapp
    type: front-end
